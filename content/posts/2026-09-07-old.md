---
tags: 经历
---

# Old

记外包项目的开发记录.

信息中心给分了一台内网云主机 ,目前只能通过 VPN进入内网管理,若要让公网用户或设备厂商访问,需要另行申请“公网 IP + 防火墙/NAT 端口开放”。

开放申请表:

- 资源内网IP:云主机在内部网络中的地址
- 对应公网IP:信息中心分配或映射的公网地址(由信息中心填写/分配)
- 对外端口号:公网允许访问的端口 (正式环境优先申请 443)
- 协议:TCP或UDP(Web、HTTPS、设备 HTTP 回调均为)
- 端口提供的具体服务

所以现在有云主机和远程访问凭据

- QA:开发文档里有提到对访问电脑的配置要求,所以我是否能用本机电脑访问?

以及我有Windows 管理员账号?

所以我现在的网络关系大致是:使用提供的运维电脑进入信息中心内网的win云主机.

这里还有提供一个win的二级密码,暂时不知道作用,可能是远程桌面或者堡垒机的密码.

正常进行对公网开放我决定通过Nginx的433端口统一承接:

- 浏览器访问
- java后端api
- sso登陆
- 前端静态资源
- 各厂商HTTP/HTTPS回调

80端口,HTTP访问重定向至443的HTTPS

根据部分IOT文档可以了解到,某品牌4G 呼叫器使用 TCP 8800，这是唯一可能需要单独申请公网端口的非 HTTP 服务。

但考虑现在真正进行测试过的IOT品牌只有一个所以暂时不考虑.

最终确认事项:

- 最终生产服务器还是临时测试资源
- “对应公网 IP”是新分配，还是已有固定公网 IP
- 公网 IP 是一对一 NAT，还是负载均衡/WAF 地址
  - 设备厂商会主动把告警发送到平台,WAF 可能把正常设备请求误认为攻击并拦截.
- 能否配置公网 443 → 内网 443，或 443 → 8443。
-  是否要求提供域名和 SSL 证书。
- 是否支持按来源 IP 配置厂商白名单
- VPN 后如何连接服务器
- 服务器能否出网访问
- 公网回调是否经过某云 WAF；若经过，需确认 WAF 是否允许 JSON POST、回调路径及请求体大小。

公网 443 → 内网服务器 443，Nginx 自己配置证书并监听 443。

## 内网环境

尝试3389和22端口,分别为远程桌面和ssh

3389远程桌面测试成功

之前没有任何接触iis的经历不过好在确认了服务器系统确实是win.(之前往linux迁移白干了)

本次使用 Windows App 通过 RDP 登录

### 操作系统和内存

```powershell
Write-Host "`n=== 1. 操作系统 ==="
Get-ComputerInfo |
  Select-Object WindowsProductName, WindowsVersion, OsBuildNumber,
                OsArchitecture, CsManufacturer, CsModel,
                CsProcessors, CsTotalPhysicalMemory
```

把字节形式的内存换算成 GB，并显示更紧凑的摘要：

```powershell
$cs = Get-CimInstance Win32_ComputerSystem
[PSCustomObject]@{
    Manufacturer = $cs.Manufacturer
    Model        = $cs.Model
    CPU          = (Get-CimInstance Win32_Processor).Name -join "; "
    Cores        = (Get-CimInstance Win32_Processor | Measure-Object NumberOfCores -Sum).Sum
    LogicalCPU   = $cs.NumberOfLogicalProcessors
    MemoryGB     = [math]::Round($cs.TotalPhysicalMemory / 1GB, 2)
    OS           = (Get-CimInstance Win32_OperatingSystem).Caption
    OSVersion    = (Get-CimInstance Win32_OperatingSystem).Version
    Architecture = (Get-CimInstance Win32_OperatingSystem).OSArchitecture
}
```

### CPU 和虚拟化

```powershell
Write-Host "`n=== 2. CPU 与虚拟化 ==="
Get-CimInstance Win32_Processor |
  Select-Object Name, AddressWidth, NumberOfCores,
                NumberOfLogicalProcessors,
                VirtualizationFirmwareEnabled,
                SecondLevelAddressTranslationExtensions
```

重点字段：

- `AddressWidth = 64`：64位系统；
- `VirtualizationFirmwareEnabled`：当前系统是否能看到固件虚拟化能力；
- `SecondLevelAddressTranslationExtensions`：是否能看到 SLAT；
- 云虚拟机中两项为 `False`，通常表示云平台没有向该虚拟机开放嵌套虚拟化。

### 磁盘容量

```powershell
Write-Host "`n=== 3. 磁盘 ==="
Get-Volume |
  Where-Object DriveLetter |
  Select-Object DriveLetter, FileSystemLabel, FileSystem,
                @{N='FreeGB';E={[math]::Round($_.SizeRemaining/1GB,2)}},
                @{N='TotalGB';E={[math]::Round($_.Size/1GB,2)}}
```

检查时不能只看总容量，还要区分：

- 系统盘：放Windows、日志和少量程序；
- 数据盘：放数据库、附件、备份和容器持久化数据；
- 空光驱或未就绪盘：可能显示 `0GB`，不应当作为数据盘使用。

### IP、DNS和网关

```powershell
Write-Host "`n=== 4. IP、DNS、网关 ==="
Get-NetIPConfiguration |
  Select-Object InterfaceAlias,
                @{N='IPv4';E={$_.IPv4Address.IPAddress -join ','}},
                @{N='Gateway';E={$_.IPv4DefaultGateway.NextHop -join ','}},
                @{N='DNS';E={$_.DNSServer.ServerAddresses -join ','}}
```

这里用于识别：

- 服务器内网 IP；
- 默认网关；
- DNS服务器；
- 是否存在多块网卡或多个地址。

云主机看到的地址通常是内网地址。浏览器使用的公网地址可能由云平台的 NAT、负载均衡或WAF映射过来，不能把内网IP直接当作公网IP。

### IIS站点和绑定

```powershell
Write-Host "`n=== 5. IIS 站点 ==="
Import-Module WebAdministration
Get-Website |
  Select-Object Name, State, PhysicalPath
```

```powershell
Write-Host "`n=== 6. IIS 绑定 ==="
Get-WebBinding |
  Select-Object protocol, bindingInformation
```

它们分别回答：

- IIS有哪些网站；
- 网站是否启动；
- 网站文件在哪个目录；
- IIS占用了哪些协议、IP、端口和主机名。

如果 `Get-Website` 不存在，先执行：

```powershell
Import-Module WebAdministration
```

### 关键端口和占用进程

```powershell
Write-Host "`n=== 7. 当前监听端口 ==="
Get-NetTCPConnection -State Listen |
  Where-Object LocalPort -in 80,443,3306,5432,6379,8040,6110,6107,11080,28080,8080,8443 |
  Sort-Object LocalPort |
  Select-Object LocalAddress, LocalPort, OwningProcess,
                @{N='Process';E={(Get-Process -Id $_.OwningProcess -ErrorAction SilentlyContinue).ProcessName}}
```

本项目相关端口的常见用途：

|  端口 | 常见用途                   | 是否建议公网开放                 |
| ----: | -------------------------- | -------------------------------- |
|    80 | HTTP统一入口               | 可以，由Nginx、IIS或负载均衡监听 |
|   443 | HTTPS统一入口              | 可以，生产环境推荐               |
|  3306 | MySQL                      | 不建议                           |
|  5432 | PostgreSQL                 | 不建议                           |
|  6379 | Redis                      | 绝对不建议                       |
|  8040 | Java后端                   | 通常仅内网或容器网络             |
|  6110 | kqco服务                   | 通常仅内网或容器网络             |
|  6107 | c TCP                      | 仅需要的内网调用方               |
| 11080 | c HTTP                     | 通常由统一入口反向代理           |
| 28080 | 管理工具                   | 不建议公网开放                   |
|  8080 | 当前Docker Nginx HTTP映射  | 调试或作为上游入口               |
|  8443 | 当前Docker Nginx HTTPS映射 | 调试或作为上游入口               |

进程显示为 `System` 且PID为4时，通常表示端口由Windows内核HTTP栈代管。结合IIS绑定可以判断是不是IIS占用。

### Docker和WSL

```powershell
Write-Host "`n=== 8. Docker、WSL ==="
docker version
docker compose version
wsl --version
wsl --status
```

这些命令不存在或报“无法识别”时，表示当前系统没有对应命令或没有完成安装，不能据此运行项目容器。

### Windows虚拟化功能

```powershell
Write-Host "`n=== 9. Windows 功能 ==="
Get-WindowsOptionalFeature -Online |
  Where-Object FeatureName -match 'Hyper-V|VirtualMachinePlatform|Microsoft-Windows-Subsystem-Linux|Containers' |
  Select-Object FeatureName, State
```

这一步查看Windows功能是否存在并启用。但需要注意：即使功能可以安装，云平台没有开放嵌套虚拟化时，内部Linux虚拟机依然可能无法启动。

---

筛查下来发现一个问题: 虚拟化能力

```text
VirtualizationFirmwareEnabled = False
SecondLevelAddressTranslationExtensions = False
```

这表示当前Windows虚拟机看不到运行嵌套虚拟化所需的能力。即使底层物理服务器支持虚拟化，也不代表该能力已经透传给当前云虚拟机。

这会直接影响：

- WSL2；
- Hyper-V虚拟机；
- 依赖Linux虚拟机的Docker Desktop后端；
- 在Windows虚拟机内部再启动Linux虚拟机。

以及此服务器使用win2016无法使用docker 桌面.

## BUG

地图无法显示BUG:

- queryYztzl 在查询智能设备数据时，调用的外部设备接口提前关闭了 HTTP 连接

原因:物理设备不在线不是问题；后端把离线/外部不可用错误地升级成 500 才是问题。
所以当前有两条独立失败链:

1. queryYztzl 失败
GssZhtzController.queryYztzl()
→ RecordServiceImpl.queryZnsb()
→ ItoServerImpl.deviceInfo()
→ 外部设备平台
→ Unexpected end of file
→ HTTP 500
queryZnsb() 对每个登记设备同步调用外部平台。任意一个设备请求失败，就终止整个统计接口。
合理行为应该是：
{
  "online": 0,
  "total": 1,
  "sbWaring": 0
}
或者附带：
{
  "status": "DEGRADED",
  "unknown": 1
}
而不是返回 500。
2. querysbListFn 阻断地图
前端初始加载顺序是：
queryYztzl()
→ /iot/jcxx/list
→ /kyfw/sb/queryList?lx=24
→ 组装 mapData
其中 /kyfw/sb/queryList 也因外部设备定位服务 SSL 握手失败返回 500。前端没有局部 try/catch，所以这一步失败后：
mapData 不再赋值
→ 地图 marker 整体不渲染
→ Axios Uncaught Promise
这正对应你看到的 querysbListFn → getMapData2 堆栈。
图标问题是第三类故障
当前运行日志确认这些图片返回 404：
/bro/img/FPSB.2f5c4752.png
/bro/img/CKDJB.6b5f9b4b.png
/bro/img/QLR.7e737911.png
此外，历史记录还确认：
部分干休所缺少 OG_CODE 和坐标，查询阶段就被过滤；
部门记录因 og_type=0 被误分类成干休所；
坐标值 "," 会通过前端 truthy 判断，随后生成 NaN；
一个“扁图标 + 干休所信息 undefined”实际来自配偶住宅记录，不是真正的干休所数据；
popup 无字段兜底，错误记录会直接显示 undefined。
历史地图修复状态
仍然有效且当前不是主因的修复：
地图 JSON /api/file/*.json 当前均返回 200；
/api/iot/ 401 路由问题已经修复；
selectWarningList 的 SQL 歧义问题已有修复记录；
Nginx 动态解析正常，当前不是这些 HTTP 500 的来源。
所以最终的修复方向为:保留设备 total，外部状态不可用时 online=0 或标记 unknown，继续处理其他设备并返回 HTTP 200。前端 getMapData 也需要隔离接口失败，否则后端某个设备源异常仍会让整张地图不组装。当前正在确认正确的后端源码工程、前端构建源和测试方式

---

阶段 1：组装离网部署包
从多个源码目录筛选出真正要上线的资产，组装成自包含的部署包：
- 产物：my-prod-production/（CoService-Windows / Backend / TianyuConsumer / Database / Nginx）
- copy-only 原则（不剪裁、不修改源文件）
- 生成清单：5,905 条 manifest、5,537 个 payload 文件
- 发现两个缺口：包里没有 nginx.exe、没有 mysqld.exe，写成占位说明
阶段 2：Windows 基础环境
MySQL 8.4.4
E:\mysql-8.4.4-winx64\bin\mysqld.exe --initialize-insecure
net start mysql
- my.ini 必须是 ASCII 无 BOM（带 BOM 会导致 MySQL 读配置失败）
- root 密码重置为 kanq@gd2024.
- 关键：认证插件换回 mysql_native_password（Java 老驱动连不上 caching_sha2_password）
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'kanq@gd2024.';
Redis
redis-server.exe --service-install redis.windows.conf --service-name ZhylCache
坑：必须用 redis.windows.conf，用 redis.windows-service.conf 会报 1067。
阶段 3：服务化（WinSW）
4 个 Java/exe 组件全部用 WinSW 2.12 包装成 Windows 服务：
服务名	组件
ZhylCache	Redis
ZhylFrontend	co-service
ZhylCoServiceMan	管理后台
ZhylBackend	Spring Boot
ZhylNginx	Nginx
WinSW XML 要点：executable / arguments / workingdirectory / logpath / <onfailure action="restart">。
坑：Spring Boot 冷启动 超过 120 秒，脚本超时要设到 300s。
阶段 4：配置适配
把所有 Docker 内网地址换成 127.0.0.1：
原值	新值	文件
10.211.55.2	127.0.0.1	config.xml / web-service.xml / database/mysql
172.16.3.39	127.0.0.1	application-prod.yml
/app/logs	E:/zhyl/logs/backend	logback
阶段 5：数据库导入 —— 最大的坑
坑 1：导入了残缺的库
第一次导入 4 月的 dump，中途出错但没加 --force，导入中断 → 缺了 oa2_organ 等表 → 页面 500：
Table 'zhyl.oa2_organ' doesn't exist
坑 2：Unknown command '\''
换 5 月 dump 重导，报：
ERROR at line 8078: Unknown command '\''.
排查过程很有意思：
1. 用 Linux 容器里的 mysql 8.0 客户端全量解析同一份 dump → 零错误，证明 dump 是好的
2. 定位到第 8078 行是 oa2_tree 的 INSERT（92,903 字节），里面是唯一含 \' 的行
3. 实测：\' 在行中间没问题，只有行首的 \ 才会被当命令
4. 结论：Windows 的 mysql 8.4.4 客户端处理超长行时会拆行，恰好断在 \' 前 → 误判为客户端命令
根因：不是数据问题，是客户端对超长行 + 反斜杠的处理差异。
坑 3：找到了"活库"这个救命稻草
排查中发现：本机 Docker 里的 MySQL 容器还在跑，里面是 7 月最新数据（508 张表），这才是和当前后端匹配的权威 schema。
对比 5 月 dump vs 活库，差异只有 2 处：
表：多 iot_watch_health（要建）
列：只缺 iot_warning.is_deleted
最终方案：直接从活库重导，并加 --skip-extended-insert（每行一条记录）：
docker exec my-prod-mysql-1 mysqldump -uroot -p'***' \
  --single-transaction --routines --triggers --events \
  --hex-blob --set-gtid-purged=OFF \
  --skip-extended-insert --complete-insert \
  zhyl > zhyl_live_safe.sql
为什么这样就成功：改后含反斜杠的行最长只有 503 字节，超 64KB 的反斜杠行 0 条 → 客户端不再拆行 → 报错消失。
阶段 6：表结构对齐
踩的坑：我一开始按"后端代码里所有用到 is_deleted 的表"批量加了 21 个列 —— 20 个是错的。
因为后端 SQL 里很多 is_deleted 是不带表名的，一旦 JOIN 的两张表都有这列 → Column 'is_deleted' in where clause is ambiguous。
教训：先对比权威 schema，再动手改。活库证明只有 iot_warning 需要补，其余全删掉。
ALTER TABLE iot_warning ADD COLUMN is_deleted int DEFAULT 0;
-- 另外 20 张 DROP COLUMN
ONLY_FULL_GROUP_BY：MySQL 8 默认开启，而后端是按 MySQL 5.7 写的 → 首页统计全 500：
sql_mode=STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION
阶段 7：地图不显示
现象：Mac 上正常，Windows 上空白且无报错。
排查：
1. 先怀疑网络 → 客户端直接打开天地图瓦片 URL → 能出图，排除网络
2. 对比 Mac 容器 vs Windows 包的同名文件（md5）→ 锁定 extends/js/zConfig.js 不一样
3. diff 出来：
101c101
< visible: true,      ← Mac
---
> visible: false,     ← Windows
> 115c115
> < visible: true,
---
> visible: false,
> 底图图层默认被关掉了 → 容器在、瓦片不发请求、不报错。
> 教训：同名文件有两份（static/bro/zConfig.js 和 static/bro/extends/js/zConfig.js），index.html 实际加载的是 extends/js/ 那份 —— 改错了完全没效果。
> 阶段 8：Nginx 上 80
> IIS 占着 80 → 之前 listen 80 报：
> bind() to 0.0.0.0:80 failed (10013)
> 步骤：
> iisreset /stop
> net stop W3SVC
> net stop WAS
> sc.exe config W3SVC start= disabled
> 然后 nginx.conf 改 listen 80 → 16 个 nginx.exe 进程监听 0.0.0.0:80（master + workers，正常）。
> *最后的"卡住"*：本机 curl http://127.0.0.1/ 返回 200，但外部访问卡死。最后用 curl 看到真相：
> < HTTP/1.1 302 Moved Temporarily
> < Server: nginx/1.30.4
> 302 跳转的目标还指向旧的 :8080（已无服务）→ 浏览器死等 → 表现为"卡住"。
> 五、踩坑清单（速查）
> #	现象	根因
> 1	MySQL 1067	用错 conf 文件
> 2	Java 连不上 MySQL	认证插件
> 3	Table 'oa2_organ' doesn't exist	导入中断
> 4	Unknown command '\''	客户端拆超长行
> 5	is_deleted ambiguous	批量补列过头
> 6	only_full_group_by	MySQL 8 默认
> 7	地图空白无报错	图层 visible:false
> 8	bind() failed 10013	IIS 占 80
> 9	页面卡住	302 跳旧端口
> 六、最终成果
- ✅ 7 个组件全部原生 Windows 服务化（开机自启 + 崩溃自愈）
- ✅ MySQL 导入 508 张表、数据完整
- ✅ 内网访问 http://192.168.235.180/（无端口号）
- ✅ 地图正常显示
- ✅ VPN-only，未暴露公网
七、经验总结
1. "活数据"是最好的文档 —— 与其猜 schema，不如直接对比一个正在运行的同版本环境。
2. 同名文件先确认加载路径 —— 有两份 zConfig.js 时，diff 和修改都要对准真正被加载的那份。
3. 报错先分离变量 —— "Mac 正常、Windows 不正常"时，先 diff 文件（最快），再查环境。
4. 客户端不是服务器 —— mysqldump 出的文件没问题，是导入端客户端的行为差异。用另一个版本/工具交叉验证能立刻定性。
5. 别批量改 —— schema 修改一定要先对比、后定点，批量操作很容易引入更隐蔽的问题。
6. 网络问题的排查顺序：本机 loopback → 本机 LAN IP → 外部，逐层排除。
